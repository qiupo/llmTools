# AGENTS.md instructions

## 工作方式

1. 一步一步思考：先明确目标、约束、现有状态、风险和验证方式，再开始行动。
2. 对可拆分的任务，先判断是否适合委派。多个相互独立、职责和文件边界清晰的调查、实现或验证工作，且并行能明显提高效率或覆盖度时，可以启动子代理。
3. 不为并发而并发。任务很小、步骤强耦合、需要共享同一运行状态，或并行修改可能互相干扰时，由主代理顺序完成。
4. 主代理始终负责拆分范围、整合结论、检查子代理改动、完成最终验证，并向用户交付一致的结果。
